//============================================================================
// UDT_Meldungen_Fehler — create this PLC data type FIRST (alone)
// Then use it as member "Fehler" inside UDT_Meldungen
//============================================================================

TYPE
    "UDT_Meldungen_Fehler" : STRUCT
        Fehlercode          : UInt;
        Fehler_Aktiv        : Bool;
    END_STRUCT;
END_TYPE
